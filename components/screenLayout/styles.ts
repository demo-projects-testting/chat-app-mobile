import { defaultStyles } from 'assets/styles';
import { StyleProp, ViewStyle } from 'react-native';

export const screenLayoutStyles = {
  paddingLeft: 36,
  paddingRight: 36,
  paddingTop: 60,
  paddingBottom: 60,
  backgroundColor: defaultStyles.whiteColor,
  flex: 1
} as StyleProp<ViewStyle>;
